<local:BaseWindow x:TypeArguments="viewmodel:MainWindowViewModel" x:Class="Baka.ContactSplitter.view.MainWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:local="clr-namespace:Baka.ContactSplitter.view" 
                  xmlns:viewmodel="clr-namespace:Baka.ContactSplitter.viewModel"
                  Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" >
            <Label>Neuer Kontakt:</Label>
            <TextBox Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}" Width="200" />
            <Button Command="{Binding AddCommand}">Hinzufügen</Button>
        </StackPanel>
        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <DataGrid Grid.Row="0"
                      Grid.Column="0" 
                      ItemsSource="{Binding ViewModelContacts}" 
                      SelectedIndex="{Binding SelectedContactIndex}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Anrede" Binding="{Binding Salutation}" />
                    <DataGridTextColumn Header="Titel" Binding="{Binding Titles}" />
                    <DataGridTextColumn Header="Vorname" Binding="{Binding FirstName}" />
                    <DataGridTextColumn Header="Nachname" Binding="{Binding LastName}" />
                    <DataGridTextColumn Header="Geschlecht" Binding="{Binding Gender}" />
                </DataGrid.Columns>
            </DataGrid>
            <Button Grid.Row="1" Grid.Column="0" Command="{Binding DeleteCommand}" HorizontalAlignment="Right">Löschen</Button>
            <Grid Grid.Row="0" Grid.Column="1" Grid.RowSpan="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Grid.Column="0">Anrede:</Label>
                <Label Grid.Row="0" Grid.Column="1" Content="{Binding SelectedContactSalutation}" />
                <Label Grid.Row="1" Grid.Column="0">Ländervorwahl:</Label>
                <Label Grid.Row="1" Grid.Column="1" Content="{Binding SelectedContactTitles}" />
                <Label Grid.Row="2" Grid.Column="0">Ortsvorwahl:</Label>
                <Label Grid.Row="2" Grid.Column="1" Content="{Binding SelectedContactFirstName}" />
                <Label Grid.Row="3" Grid.Column="0">Nummer:</Label>
                <Label Grid.Row="3" Grid.Column="1" Content="{Binding SelectedContactLastName}" />
                <Label Grid.Row="4" Grid.Column="0">Durchwahl:</Label>
                <Label Grid.Row="4" Grid.Column="1" Content="{Binding SelectedContactGender}" />
            </Grid>
        </Grid>
    </Grid>
</local:BaseWindow>
