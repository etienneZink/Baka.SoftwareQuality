<local:BaseWindow x:TypeArguments="viewmodel:MainWindowViewModel" x:Class="Baka.ContactSplitter.view.MainWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:local="clr-namespace:Baka.ContactSplitter.view" 
                  xmlns:viewmodel="clr-namespace:Baka.ContactSplitter.viewModel"
                  Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" >
            <Label>Neuer Kontakt:</Label>
            <TextBox Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}" Width="200" />
            <Button Command="{Binding AddCommand}">Hinzufügen</Button>
        </StackPanel>
        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Grid.Column="0" Grid.RowSpan="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Visibility="{Binding ParsedContactDetailsVisible}">Anrede:</Label>
                <Label Grid.Row="0" Grid.Column="1" Visibility="{Binding ParsedContactDetailsVisible}" Content="{Binding ParsedSalutation}" />
                <Label Grid.Row="1" Grid.Column="0" Visibility="{Binding ParsedContactDetailsVisible}">Titel:</Label>
                <Label Grid.Row="1" Grid.Column="1" Visibility="{Binding ParsedContactDetailsVisible}" Content="{Binding ParsedTitles}" />
                <Label Grid.Row="2" Grid.Column="0" Visibility="{Binding ParsedContactDetailsVisible}">Vorname:</Label>
                <Label Grid.Row="2" Grid.Column="1" Visibility="{Binding ParsedContactDetailsVisible}" Content="{Binding ParsedFirstName}" />
                <Label Grid.Row="3" Grid.Column="0" Visibility="{Binding ParsedContactDetailsVisible}">Nachname:</Label>
                <Label Grid.Row="3" Grid.Column="1" Visibility="{Binding ParsedContactDetailsVisible}" Content="{Binding ParsedLastName}" />
                <Label Grid.Row="4" Grid.Column="0" Visibility="{Binding ParsedContactDetailsVisible}">Geschlecht:</Label>
                <Label Grid.Row="4" Grid.Column="1" Visibility="{Binding ParsedContactDetailsVisible}" Content="{Binding ParsedGender}" />
            </Grid>
            <DataGrid Grid.Row="0"
                      Grid.Column="1" 
                      ItemsSource="{Binding ViewModelContacts}" 
                      SelectedIndex="{Binding SelectedContactIndex}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Anrede" Binding="{Binding Salutation}" />
                    <DataGridTextColumn Header="Titel" Binding="{Binding Titles}" />
                    <DataGridTextColumn Header="Vorname" Binding="{Binding FirstName}" />
                    <DataGridTextColumn Header="Nachname" Binding="{Binding LastName}" />
                    <DataGridTextColumn Header="Geschlecht" Binding="{Binding Gender}" />
                </DataGrid.Columns>
            </DataGrid>
            <Button Grid.Row="1" Grid.Column="1" Command="{Binding DeleteCommand}" HorizontalAlignment="Right">Löschen</Button>
        </Grid>
    </Grid>
</local:BaseWindow>
